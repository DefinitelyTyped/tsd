:warning: The `/test` directory only holds fixture data (and some simple tests). 

:point_right: The specs are located in `/src/spec` and supporting code in `/src/test` (as an experiment to manage the relative paths and partial compilation mess).
