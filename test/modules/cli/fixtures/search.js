/*jshint -W098*/
var lib = require('../../../lib/lib');

var tests = {
	'async': {
		//debug: true,
		selector: {
			pattern: 'async'
		}
	}
};

module.exports = {
	command: ['search'],
	tests: tests
};